export const mapCategory = (old) => {
  return {
    // NEW : OLD
    name: old.id === 18 ? 'Sports Life' : old.name,
    // status: old.status,
    // addedBy: old.creator,
  }
}
// categories

// id ->
// name ->  name
// status ->
// created ->
// creator ->
// updated ->
// updater ->

// name
// redirecturl(internal/external)
// link
// added by
// updated by
// children
// status
// photodark-
// photolight
